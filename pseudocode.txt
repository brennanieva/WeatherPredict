

for i in range(1, len(training_data)):
    today = training_data[i]
    yesterday = training_data[i-1]

    lin_predictions.append(predict_linear(yesterday))
    eq_predictions.append(predict_equal(yesterday))

    lin_errors.append(compare_predictions(lin_pred, today))
    eq_errors.append(compare_predictions(eq_pred, today))


avg_error = compute_mean(lin_errors)



11/26 notes

Prediction function "API":

A prediction function should look like the following:

def predict_somehow(training_data, index):
    """ Predict the temperature of day training_data[index]
    based on the days in training_data[:index] """


12/3 notes

- avoid referencing global variables; give prediction functions the interface described above
- csv file parser
- compute mean absolute error instead of mean signed error
- do calculations on floating point numbers instead of ints
